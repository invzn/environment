---
description: Tech lead gathers context via repo-expert and produces a TDD plan — prioritized behaviors to test (no implementation)
---
Use the subagent tool to delegate to the "tech-lead" agent with this task:

Analyze and plan (do NOT implement) the following using vertical-slice TDD methodology: $@

1. Delegate to "repo-expert" to gather all relevant code and context
2. Produce a TDD plan: the tracer bullet (first behavior to drive end-to-end), a prioritized list of behaviors to test, an interface sketch (signatures, not implementation), and deep-module opportunities — NOT a function-by-function implementation plan

Stop after planning — do not proceed to the TDD loop or review.
